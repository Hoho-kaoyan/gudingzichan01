# 已完成工作（按时间线）

本文档按 Git 提交时间线整理**当前分支已完成的开发工作**，便于投产验收与回归。详细实现见代码与本文档第二、三节。

---

## 一、时间线与完成项

| 阶段/时间线 | 完成内容 | 对应提交/说明 |
|-------------|----------|----------------|
| **联动安全检查基础** | 联动表与 Model、解析逻辑（按实物名称）、配置 API；asset_name 为 None 的 bug 修复 | 人员A：safety_check_auto_config、SafetyCheckAssetTypeMapping、get_check_type_for_asset |
| **入库与交接联动** | 场景一（入库）与场景二情况一（交接）联动；创建系统任务服务 | 场景一/二情况一：create_system_allocated_task、入库/交接前校验 |
| **方案乙与状态统一** | 在库筛选（user_id=仓库用户）、状态「库存备用」→「在库」 | 方案乙、状态用语统一 |
| **调拨联动与延迟生效** | 修改使用人走调拨联动；PendingReallocation；任务完成后使用人/组别生效 | 调拨联动、待生效调拨 |
| **资产调拨与交互** | 调拨联动完善、EHR 浮点解析、组长角色选项等前端修复 | 前端+后端联调 |
| **任务列表与联动提示** | 任务列表按来源筛选；交接/退回/调拨遇「需先完成数据安全检查」时弹窗提示并引导至「我的检查任务」 | 4.1～4.4 功能点 |
| **用户逻辑删除** | User 表 deleted_at/deleted_by_id；删除接口改为置空；查询统一过滤 deleted_at | T1 |
| **资产导入恢复+扩展** | 导入时「存在且已逻辑删除」则恢复并更新该行；Asset 扩展字段；所有人/所有人ID/使用人EHR、安全检查执行人、列名兼容；派单用 safety_check_executor_id | T2、T3、T4 |
| **组长权限与列表** | get_assets 组长仅看本组；get_asset 组长可看本组资产；create/update 组长校验；标记离职接口 | 任务12、任务13 |
| **文档与测试修订** | D1/D2/D3 落实；已完成与待办分离；测试主清单明确；测试文档密码统一为 1234567 | 文档修订意见落地 |

---

## 二、按模块汇总（便于验收）

| 模块 | 已完成功能点 |
|------|--------------|
| **认证与用户** | 用户逻辑删除；get_user_by_ehr 过滤已删除；标记离职接口；用户列表/详情/更新/导入过滤 deleted_at |
| **资产** | 状态「在库」统一；在库筛选（方案乙）；扩展字段与导入列名兼容；导入「恢复+更新」；组长列表与详情权限；调拨联动与待生效调拨 |
| **交接/退回/审批** | 状态描述「在库」；退回后未完成任务标记已退库；编辑审批改使用人时任务跟到新接收人 |
| **安全检查** | 联动配置（默认类型+实物名称→检查类型）；入库/交接/调拨/离职联动；任务完成后调拨生效 |
| **统计** | 资产总数/在用数过滤已删除资产 |
| **文档与测试** | 核心功能测试优先清单为主清单；测试用例文档合并；批量导入指南与扩展方案同步；人员A解析差异表述修正 |

---

## 三、参考文档

- [核心功能投产范围与确认事项](../方案与报告/核心功能投产范围与确认事项.md)
