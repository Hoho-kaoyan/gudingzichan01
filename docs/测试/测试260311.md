# 固定资产管理系统 - 测试文档 260311

## 文档信息

| 项目 | 内容 |
|------|------|
| **文档名称** | 固定资产管理系统测试文档（含联动与我的安全检查任务细化） |
| **文档编号** | 测试260311 |
| **编写日期** | 2026年3月11日 |
| **测试范围** | 登录、首页、用户管理、资产管理（含批量导入/冲突处理/编辑申请）、资产交接（含联动校验）、资产退回、审批管理、安全检查任务管理、我的安全检查任务、联动任务类型配置；方案乙（在库筛选/状态在库）；组长角色与调拨联动 |
| **测试目的** | 在 260304 文档基础上，对核心联动流程和前端新细化能力进行更完整、可回归的验证 |

---

## 一、测试环境与账户

### 1.1 系统要求

- **后端服务**：`http://localhost:8000`
- **前端服务**：`http://localhost:3000` 或 `http://localhost:5173`
- **浏览器**：Chrome / Edge / Firefox（最新版本）

### 1.2 测试账户

| 角色 | EHR号 | 说明 |
|------|--------|------|
| 管理员 | 0000001 | 默认系统管理员（`init_db.py` 创建） |
| 组长 | 1234567 | 测试组长1（与部分用户同组，用于验证组长权限与调拨联动） |
| 普通用户A | 1234568 | 测试用户1（有名下资产） |
| 普通用户B | 1234569 | 测试用户2（用于交接/调拨场景） |

**测试前准备：**
1. 后端、前端服务已启动。
2. `python backend/init_db.py` 已执行完成，基础数据存在。
3. 至少准备以下数据：
   - 若干资产大类。
   - 管理员名下或仓库名下「在库」资产若干。
   - 普通用户和组长名下「在用」资产若干（用于交接/安全检查）。

---

## 二、模块与回归范围总览（与 260304 差异点）

| 模块 | 主要功能点 | 本次新增/强化关注点 |
|------|------------|---------------------|
| 登录/首页 | 与 260304 一致 | 仅做冒烟回归 |
| 用户管理 | 列表、筛选、角色、标记离职 | 标记离职与联动任务说明保持一致 |
| 资产管理 | 列表/筛选（方案乙）、新增/编辑/删除、批量导入/导出、资产详情、编辑申请 | **新增：批量导入错误详情/冲突处理 UI、编辑申请状态列与详情、资产详情 Modal 展示** |
| 资产交接 | 申请/确认/审批 | **强化：与安全检查联动校验（交接前必须完成任务）** |
| 审批管理 | 交接/退回/编辑申请审批 | 与 260304 一致，补充编辑申请通过后数据正确性验证 |
| 安全检查任务管理 | 管理员发布任务、联动配置 Modal | 与 260304 一致，重点与「我的安全检查任务」联动 |
| 我的安全检查任务 | 待检查/已完成 Tab、任务卡片、执行检查、按资产分步提交 | **新增：多资产任务分步检查、步骤条状态、已检查资产回显、联动调拨生效触发** |
| 联动任务配置 | Modal 配置默认检查类型与名称映射 | 与核心功能优先清单保持一致 |

> **说明**：若 260304 中已有相同用例，本文件仅对新增/修改点补充更细步骤，可与 260304 交叉引用使用。

---

## 三、资产管理补充测试（导入错误/冲突 & 资产详情 & 编辑申请）

### 3.1 批量导入错误明细与失败记录弹窗

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-ASSET-IMP-ERR-01 | 批量导入-基础错误明细展示 | 管理员已登录；准备一个 Excel，包含若干行缺少必填列/格式错误 | 1. 进入`资产管理` 2. 点击`批量导入` 3. 选择含错误行的 Excel 4. 上传完成后观察提示及弹窗 | 1）页面顶部展示导入结果消息，说明成功/失败/跳过条数 2）若存在失败条数，自动弹出「导入失败记录详情」Modal 3）Modal 头部 Alert 显示失败总数 4）表格列包含「行号/失败原因/资产编号/所属大类/实物名称/规格型号/状态/使用人EHR号/其他数据」等字段 5）可分页查看，行号与 Excel 实际行号一致或有清晰对照逻辑 | 高 |
| TC-ASSET-IMP-ERR-02 | 批量导入-失败原因与原始行数据完整性 | 同上 | 1. 在错误行中故意设置：缺失资产编号/无效大类/状态非法值等 2. 导入后打开失败记录详情 3. 展开任意一条记录的「其他数据」 | 1）`失败原因`列能用中文明确描述失败原因 2）展开行后能看到该 Excel 行的全部原始字段与值（过滤掉空值或 `nan`） 3）关闭 Modal 再次导入不会残留旧错误数据 | 中 |

### 3.2 批量导入冲突处理（与数据库有差异）

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-ASSET-IMP-CONF-01 | 导入冲突-弹窗展示差异字段 | 管理员已登录；数据库中已有若干资产；Excel 中使用相同资产编号，但修改了部分字段（如规格、存放地点） | 1. 资产管理 → 批量导入 2. 上传包含与现有资产发生字段差异的 Excel 3. 等待导入完成 | 1）导入完成后如存在冲突，弹出「导入冲突：请选择覆盖或保持」Modal 2）每条冲突记录以「第X行 · 资产编号」为标题块 3）内部表格展示字段、数据库当前值、导入文件中的值三列，颜色区分（蓝色/绿色） 4）顶部说明文案提示用户需逐条选择「覆盖」或「保持」 | 高 |
| TC-ASSET-IMP-CONF-02 | 导入冲突-选择覆盖数据库 | 同上 | 1. 在冲突弹窗中，选择某条记录点击`覆盖（用导入数据更新）` 2. 等待接口返回 3. 在资产列表中查找该资产 | 1）操作成功后 toast 提示「已用导入数据覆盖资产XXX」 2）该冲突记录从弹窗中消失，当所有冲突处理完时自动关闭弹窗 3）资产列表中该资产的相关字段更新为导入文件中的值 4）刷新页面后数据保持一致 | 高 |
| TC-ASSET-IMP-CONF-03 | 导入冲突-选择保持数据库 | 存在至少1条冲突记录 | 1. 在冲突弹窗中选择某条记录点击`保持（不改动数据库）` 2. 完成后关闭弹窗 3. 检查资产列表 | 1）不会更新数据库中已有资产字段 2）该冲突记录从弹窗列表中移除 3）资产列表数据保持导入前状态 4）再次导入同一 Excel 时，仍按当前实现重新触发冲突判断（根据系统设计） | 中 |

### 3.3 资产详情 Modal 展示与状态文案（方案乙）

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-ASSET-DETAIL-01 | 列表中查看资产详情 | 任意登录用户；存在若干资产 | 1. 打开`资产管理` 2. 点击某一行操作列中的`查看详情`图标 3. 观察弹出的「资产详情」Modal | 1）Modal 标题为「资产详情」，宽度适中，支持滚动 2）内容使用`Descriptions`方式展示，与 `ASSET_FORM_FIELDS` 中字段一致（资产编号、所属大类、实物名称、规格、状态、IP/MAC、存放地点、楼层、座位号、使用人、组别等） 3）使用人字段以「姓名 (EHR号：xxx)」展示，在库资产显示为「在库」 4）创建时间/更新时间格式为 `YYYY-MM-DD HH:mm:ss` | 中 |
| TC-ASSET-DETAIL-02 | 状态文案统一为在库/在用 | 数据中存在状态为「库存备用」的历史资产 | 1. 在列表中找到状态为「库存备用」或「在库」的资产 2. 观察列表 Tag 文案 3. 打开该资产详情 Modal 再次观察状态字段 | 1）列表中：凡数据库状态为「库存备用」或「在库」，均以灰色 Tag 文案「在库」展示；在用为绿色 Tag「在用」 2）详情 Modal 中：状态字段同样将「库存备用」归一展示为「在库」 3）未出现「库存备用」字样 | 高 |

### 3.4 资产编辑申请（普通用户）与状态列

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-ASSET-EDITREQ-01 | 普通用户提交编辑申请 | 普通用户登录；名下有「在用」资产；后端支持编辑申请流程 | 1. 以普通用户A登录 2. 进入`资产管理` 3. 在名下资产行点击`编辑` 4. 修改备注/存放地点等字段 5. 保存 | 1）保存后提示「编辑申请已提交，等待管理员审批」 2）Modal 自动关闭，列表刷新 3）该资产的「编辑申请」列显示状态 Tag「待审批」 4）再次尝试编辑该资产时，给出“已有待审批编辑申请，请等待或撤回”的提示并禁止继续编辑 | 高 |
| TC-ASSET-EDITREQ-02 | 编辑申请详情与撤回 | 上述用例已提交至少1条待审批的编辑申请 | 1. 在资产列表中找到带有「待审批」状态的资产 2. 在「编辑申请」列点击「详情」图标 3. 在弹出的「编辑申请详情」中检查申请时间、审批状态、申请修改字段等 4. 关闭详情后再次打开详情 5. 在详情中或列表中点击「撤回」按钮并确认 | 1）详情中展示资产编号/名称、申请时间、当前审批状态、审批人及审批时间（如有）、审批意见等 2）「申请修改的字段」列表中展示字段中文名与期望值；用户/大类字段显示人名/大类名而非ID 3）点击撤回后提示「编辑申请已撤回」，资产列表中编辑申请状态列清空或不再显示，用户可重新发起编辑 | 中 |

---

## 四、资产交接与安全检查联动补充测试

以下用例在 260304 的基础上，结合 `TransferManagement.jsx` 与安全检查联动错误解析逻辑进行细化。

### 4.1 交接申请-基础流程回归（简版）

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-TRANS-BASE-01 | 普通用户申请交接 | 普通用户A登录；名下有在用资产；存在普通用户B | 1. 进入`资产交接` 2. 点击`申请交接` 3. 在资产下拉中仅能选择自己名下的在用资产 4. 通过自动补全选择普通用户B为转入人 5. 填写交接原因 6. 提交 | 1）提交成功，toast「交接申请已提交」 2）列表新增一条记录，状态为「待转入人确认」，转出人=用户A，转入人=用户B 3）转出人/管理员在该条记录上有「撤回」操作按钮 | 高 |

### 4.2 交接确认与审批-状态与意见展示

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-TRANS-CONFIRM-01 | 转入人确认交接 | 存在状态为待转入人确认且转入人为用户B的申请 | 1. 以用户B登录 2. 打开`资产交接` 3. 在待确认记录操作列点击`确认` 4. 弹窗中选择「确认接收」，可填写备注，提交 | 1）提交成功提示「已确认交接申请」 2）记录状态变为「待审批」，转入人意见列显示「✓ 已确认」Tag，并展示备注与确认时间 3）刷新页面后数据保持一致 | 中 |
| TC-TRANS-CONFIRM-02 | 转入人拒绝交接 | 同上 | 1. 再创建一条新申请 2. 以转入人登录并选择「拒绝接收」，填写原因 3. 提交 | 1）记录状态变为「转入人已拒绝」 2）转入人意见列显示「✗ 已拒绝」Tag，备注可展开/收起，展示完整原因与时间 3）转出人/管理员不再显示撤回按钮 | 中 |

### 4.3 交接与安全检查联动校验（需先完成检查）

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-TRANS-LINK-01 | 交接提交触发安全检查未完成拦截 | 后端已开启交接前安全检查联动规则；某资产名下存在未完成的安全检查任务 | 1. 以资产当前使用人登录 2. 在`资产交接`中选择该资产并选择任意转入人 3. 提交交接申请 | 1）若未完成联动安全检查，接口返回联动错误；前端通过 `parseSafetyCheckError` 识别 2）弹出 Modal 提示「需要先完成数据安全检查」，文案中包含任务编号/说明（按实际实现） 3）Modal 有「前往我的检查任务」按钮，点击跳转到`/my-safety-check-tasks` 页面 4）列表中不产生新的交接记录（或保持原状态，依实现） | 高 |
| TC-TRANS-LINK-02 | 转入人确认时联动安全检查拦截 | 已存在交接申请；资产或人员存在未完成安全检查任务；转入人准备确认 | 1. 以转入人登录 2. 在`资产交接`中找到待确认记录，点击`确认` 3. 在弹窗中选择「确认接收」并提交 | 1）若存在联动检查未完成，接口返回错误信息 2）前端关闭确认弹窗并弹出「需要先完成数据安全检查」说明 Modal 3）点击「前往我的检查任务」跳转到`我的安全检查任务`页面 4）交接记录状态仍为「待转入人确认」 | 高 |
| TC-TRANS-LINK-03 | 完成安全检查后交接流程正常 | 承接上两条用例；相关安全检查任务已全部完成且状态更新 | 1. 在`我的安全检查任务`中完成相关任务 2. 返回`资产交接`页 3. 再次发起交接申请或再次进行转入人确认与管理员审批 | 1）交接申请/确认/审批流程可正常通过 2）审批通过后资产使用人更新为转入人，历史记录中有完整流转记录 3）侧边栏待确认/待审批数量正确更新 | 高 |

---

## 五、我的安全检查任务（MySafetyCheckTasks）详细用例

本章节重点覆盖前端 `MySafetyCheckTasks.jsx` 实现中的行为，包括任务列表展示、Tab 过滤、任务详情与逐资产检查提交、联动更新待检查数量与调拨生效。

### 5.1 任务列表与 Tab 统计

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-LIST-01 | 待检查/已完成/全部 Tab 统计 | 普通用户登录；后端接口 `/safety-check-results/my-tasks` 返回至少2条 pending_count>0、1条 pending_count=0 的任务 | 1. 访问`我的安全检查任务`页 2. 观察顶部 Tab 文案「待检查(x)/已完成(y)/全部」 3. 切换到「待检查」Tab 4. 切换到「已完成」Tab 5. 切换到「全部」Tab | 1）页面初始化时自动加载全部任务；控制台日志中可看到 fetch 成功（仅用于开发者） 2）「待检查」Tab 文案中的数字等于 `pending_count>0` 的任务数量，「已完成」为 `pending_count===0` 数量 3）在不同 Tab 间切换时，Tab 上的数量不会变成 0（因为是基于完整 tasks 计算） 4）「待检查」列表只展示 pending_count>0 的任务，「已完成」只展示 pending_count=0 的任务，「全部」展示所有 | 中 |

### 5.2 任务卡片信息展示

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-CARD-01 | 任务卡片字段及样式 | 普通用户有若干任务；部分任务设置了截止时间 | 1. 在任意 Tab 下查看任务列表 2. 观察卡片上的标题、状态、编号、检查类型、待检查数、截止时间等 | 1）每个任务以 Card 展示，顶部为任务标题，右上角 Tag 显示「待检查」或「已完成」，逾期任务额外显示「已逾期」Tag 2）中部依次展示：任务编号、检查类型名称、待检查资产数量、截止时间 3）底部区域左侧为`查看详情`按钮，右侧为`开始检查`主按钮（仅待检查任务显示） 4）卡片 hover 时有阴影与边框高亮效果（样式符合设计） | 低 |

### 5.3 打开任务详情并加载资产检查项

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-DETAIL-01 | 打开任务详情 & 加载资产列表 | 普通用户登录；存在至少一条待检查任务，任务下有多条资产 | 1. 在「待检查」Tab 中点击任一任务卡片的`开始检查`或`查看详情`按钮 2. 观察弹出的 Modal 内容 | 1）前端调用`GET /safety-check-results/task/{taskId}/assets`，若资产列表为空则提示「该任务没有分配的资产」并不打开 Modal 2）若有资产，弹出标题为「安全检查: {任务编号或标题}」的 Modal 3）顶部为 Steps 步骤条，按资产数量展示多个步骤，当前资产标记为 `process`，已检查的资产为 `finish`，未检查的为 `wait` 4）中部 Card 标题为「资产: 资产编号 - 名称」，并展示使用人、存放地点/楼层等信息 5）检查类型信息来自接口返回的 `check_type`，在后续检查项表单中使用 | 高 |

### 5.4 单资产检查项填写与提交

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-CHECK-01 | 必填检查项与备注填写 | 某任务下配置的检查类型包含多个检查项，部分标记为必填 | 1. 在任务详情 Modal 中查看当前资产检查项列表 2. 对于有「必填」Tag 的检查项，不勾选任何结果尝试提交 3. 对所有检查项选择「是/否」，并在部分项填写备注 | 1）每个检查项以卡片展示，标题为「序号. 检查项内容」，右侧可能有红色「必填」Tag 2）未对所有必填项选择结果时，点击「提交检查结果」按钮会触发表单校验，提示「请选择检查结果」 3）可在每个检查项下方的备注框中填写文本，非必填 4）检查备注（整体）为单独的文本框，可选填 | 高 |
| TC-MYSAFE-CHECK-02 | 提交单资产检查结果并更新本地状态 | 同上 | 1. 对当前资产所有必填检查项选择结果（至少一项选「否」）并填写/不填写备注 2. 点击`提交检查结果` 3. 观察提示与 Steps/按钮变化 | 1）成功后 toast 提示「检查结果提交成功」 2）当前资产对象在前端状态中被标记为 `status: 'checked'`，并保存 `check_result`（含整体 yes/no）、`check_comment` 与 `check_items_result` 数组 3）步骤条中当前资产步骤变为「已完成」状态 4）若还有未检查资产，自动跳转到下一条待检查资产；若无则关闭 Modal 并自动刷新任务列表 | 高 |

### 5.5 多资产任务逐条检查与数据回显

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-MULTI-01 | 在多资产任务中前后跳转 | 某任务下至少有3条资产；至少为前2条提交过检查结果 | 1. 打开任务详情 Modal 2. 对第1资产完成检查并提交 3. 系统自动跳转到第2资产 4. 点击`上一个`和`下一个`按钮在资产间切换 | 1）提交第1资产后自动聚焦到第一个 `status='pending'` 的资产 2）点击`上一个`和`下一个`可在资产之间切换，越界时按钮自动隐藏 3）切换资产时，表单内容根据当前资产的 `check_items_result` 与 `check_comment` 自动回显（已检查资产显示之前选择的 yes/no 和备注） 4）对已检查资产不再显示「提交检查结果」按钮，仅显示绿色「已检查」Tag | 中 |
| TC-MYSAFE-MULTI-02 | 重新打开任务详情后的数据持久性 | 前端使用同一页面会话；某任务下已有部分资产检查完成 | 1. 关闭任务详情 Modal 2. 在任务卡片上再次点击`查看详情` 3. 在 Steps 中点选不同资产，观察表单 | 1）重新打开后 Steps 中仍然准确标记已完成/待检查状态（基于接口返回数据） 2）对已检查资产，表单中各检查项与备注按照最新结果回显 3）待检查资产表单为空白（未保存过结果） | 中 |

### 5.6 与待检查数量/联动调拨的联动

| 用例ID | 用例名称 | 前置条件 | 测试步骤 | 期望结果 | 优先级 |
|--------|----------|----------|----------|----------|--------|
| TC-MYSAFE-LINK-01 | 完成任务后待检查数量同步 | 有多个任务；系统侧边栏或其他位置展示待检查任务数量（`useTransfer` 上下文） | 1. 记录当前侧边栏待检查任务数量 2. 在`我的安全检查任务`中完成某个任务下所有资产检查 3. 关闭 Modal 4. 观察任务列表和侧边栏数量 | 1）完成最后一个资产检查后，任务整体从「待检查」Tab 移动到「已完成」Tab 2）`syncPendingSafetyCheckCountFromTasks` 与 `refreshPendingSafetyChecks` 被调用后，侧边栏待检查数量同步减少 3）刷新页面后数量保持一致 | 中 |
| TC-MYSAFE-LINK-02 | 调拨联动任务完成后使用人变更 | 后端已支持调拨联动逻辑；存在因修改资产使用人而生成的待生效调拨任务及对应安全检查任务 | 1. 管理员或组长在`资产管理`中修改某资产使用人，使其触发联动安全检查任务（待生效调拨） 2. 以任务执行人登录，前往`我的安全检查任务`完成该调拨联动任务全部检查 3. 返回`资产管理`查看该资产使用人与待生效调拨记录 | 1）在安全检查任务全部完成后，资产使用人更新为新使用人 2）该资产的待生效调拨记录被清除或状态标记为已生效 3）相关历史记录中保留变更过程 4）如有缓存或筛选条件，刷新列表后数据一致 | 高 |

---

## 六、联动任务配置补充说明（与核心功能优先清单对齐）

**目标**：确保管理员可通过联动配置 Modal 完整配置检查类型与实物名称映射，并与任务发布、资产导入、交接/调拨联动场景联通。

| 用例ID | 用例名称 | 说明 |
|--------|----------|------|
| TC-CFG-01～04 | 打开联动配置、保存全局默认检查类型、新增/编辑/删除映射 | 用例步骤与预期参考《核心功能测试优先清单》第二节，注意确认：再次打开 Modal 时配置可正确回显；删除映射后该实物名称回退为使用全局默认类型或不创建任务（按设计） |
| TC-ASSET-IMPORT-02 | 批量导入后自动创建安全检查任务 | 参考核心功能优先清单第五节，重点确认：导入带使用人的资产时，创建的安全检查任务在`我的安全检查任务`中可正常展示并执行 |
| TC-TRANS-LINK-* | 交接与联动校验 | 参考本文件第四章与核心功能优先清单第六节，对照确认具体联动行为一致 |

---

## 七、回归与执行记录（建议格式）

| 章节 | 执行日期 | 执行人 | 通过/总数 | 备注 |
|------|----------|--------|-----------|------|
| 三、资产管理补充 | ____/____/____ | ________ | __/7 | |
| 四、资产交接联动 | ____/____/____ | ________ | __/6 | |
| 五、我的安全检查任务 | ____/____/____ | ________ | __/8 | |
| 六、联动配置补充 | ____/____/____ | ________ | __/3 | |

---

## 八、与其他文档的关系

- **《测试文档260304》**：覆盖基础功能与主要流程，本文件在其基础上增加针对前端新特性与联动细节的补充用例。执行完整回归时建议两份文档结合使用。
- **《核心功能测试优先清单》**：定义核心范围与优先级，本文件中与联动配置/批量导入/联动场景相关的用例已与该清单编号对齐，可交叉引用。

